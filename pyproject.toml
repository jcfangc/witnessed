[project]
name = "witnessed"
version = "0.1.0"
description = "Proof-carrying values for enforcing invariants across decoupled functions."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pre-commit>=4.5.1"]
